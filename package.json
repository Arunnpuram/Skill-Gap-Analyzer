{
  "name": "skill-gap-analyzer",
  "version": "1.0.0",
  "description": "Privacy-first Chrome extension for skill gap analysis",
  "main": "dist/background.js",
  "scripts": {
    "build": "webpack --mode=production",
    "dev": "webpack --mode=development --watch",
    "lint": "eslint src/**/*.ts",
    "type-check": "tsc --noEmit",
    "package": "npm run build && npm run package:prepare && npm run package:zip",
    "package:prepare": "powershell -Command \"if (Test-Path 'extension-package') { Remove-Item -Recurse -Force 'extension-package' }; New-Item -ItemType Directory -Path 'extension-package'; Copy-Item 'dist/background.js' 'extension-package/'; Copy-Item 'dist/content.js' 'extension-package/'; Copy-Item 'dist/popup.js' 'extension-package/'; Copy-Item 'manifest.json' 'extension-package/'; Copy-Item 'popup.html' 'extension-package/'; if (Test-Path 'src/styles') { Copy-Item -Recurse 'src/styles' 'extension-package/' }\"",
    "package:zip": "powershell -Command \"Compress-Archive -Path 'extension-package/*' -DestinationPath 'skill-gap-analyzer-extension.zip' -Force\""
  },
  "keywords": [
    "chrome-extension",
    "skills",
    "job-search",
    "privacy"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^8.0.0",
    "copy-webpack-plugin": "^11.0.0",
    "css-loader": "^6.8.1",
    "eslint": "^8.49.0",
    "style-loader": "^3.3.3",
    "ts-loader": "^9.4.4",
    "typescript": "^5.2.2",
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "compromise": "^14.10.0"
  }
}
